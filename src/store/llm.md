需要新建一个 zustand 状态进行管理

需要管理三个状态

- llm 模型的初始化和释放
- embedding 模型的初始化和释放
- rag 模型的初始化和释放（这个知识单纯的数据库？）但是依赖 embedding

插件侧对 C 返回的 char\* 统一做“立即拷贝并释放”，前端不持有/不释放指针

然后提供接口

在切换为端侧模式的时候直接初始化端侧模型，在需要的时候提供 response 函数，然后在关闭端侧模型的时候释放模型

rag 逻辑暂时不做，需要新增较多UI

因为向量检索的存在，软件需要的时候再做 rag 的初始化，如果很长时间没用就释放

做检索的时候检查一下，如果是释放状态就重新 init

因为 embedding 和 rag 基本上是强绑定的，所以这两的 init 和 release 需要封装一下

---

鉴于这个关系到全局状态管理，所以把逻辑都放在前端，如果 rust 侧的调用需要 rag 支持，就把检索到的向量作为 prompt 一并传回去

理论上不会遇到并发处理的情况，但是如果有就按照单飞锁的情况处理

---

在App.tsx中初始化配置

AIPlanner中新增端侧状态按钮，触发初始化和realse

根据状态隐藏部分接口，改用新的端侧接口
